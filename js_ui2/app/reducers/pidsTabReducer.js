import * as t from '../action_types'

const pidsTabInitialState = {
}

export default function (state = pidsTabInitialState, action) {
  const payload = action.payload

  switch (action.type) {
    default:
      return state
  }
}
